
<div class="row justify-content-center" style="border:1px black solid;">
    <div class="title col-6">
        <h1>L.C.D.P.R <br/>(Le cercle des Poètes retrouvés)</h1></div>
    <div class="citation col-12 col-md-4 row align-items-end">
        <blockquote>" La vertu paradoxale de la lecture est de nous abstraire du monde pour lui trouver un sens." <br />
            <em>Daniel Pennac</em></blockquote>
        <img src="#" alt="">
    </div>
</div>

<nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top">
    <img src="{{ asset('images/livre.jpg') }}" alt="Logo" style="width:80px;">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo02" aria-controls=   "navbarTogglerDemo02" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarTogglerDemo02">
        <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
            {% if app.request.get('_route') not in ['homepage', 'entries'] %}
            <li class="nav-item enablee">
                <a class="nav-link" href="{{ path("homepage") }}">Home<span class="sr-only">(current)</span></a>
            </li>
            {% endif %}
            {% if app.user %}
            <li class="nav-item enable">
                <a class="nav-link" href="{{ path("admin_index") }}">Admin</a>
            </li>
            <li class="nav-item enable">
                <a class="nav-link" href="{{ logout_url("secured_area") }}">Deconnexion</a>
            </li>
            {% else %}
            <li class="nav-item enable">
                <a class="nav-link" href="/connect/auth0">Se connecter</a>
            </li>
        </ul>
        {% endif %}

    </div>
</nav>
</header>











<nav class="navbar navbar-default ">
    <div id="navbar" class="collapse navbar-collapse pull-right">
        <ul class="nav navbar-nav">
            {% if app.request.get('_route') not in ['homepage', 'entries'] %}
                <li><a href="{{ path("homepage") }}">Home</a></li>
            {% endif %}
            {% if app.user %}
                <li><a href="{{ path("admin_index") }}">Admin</a></li>
                <li><a href="{{ logout_url("secured_area") }}">Logout</a></li>
            {% else %}
                <li class="active"><a href="/connect/auth0">Login</a></li>
            {% endif %}
        </ul>
    </div>
</nav>